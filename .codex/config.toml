# Project-scoped Codex configuration for VisiForm.

[agents]
# Maximum number of open agent threads, including the lead session.
max_threads = 6

# Allow direct subagents while preventing recursive agent fan-out.
max_depth = 1
